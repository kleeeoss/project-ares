apiVersion: v1
kind: Service
metadata:
  name: coordinator-service
  namespace: ares
spec:
  type: LoadBalancer
  selector:
    app: ares-coordinator
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000